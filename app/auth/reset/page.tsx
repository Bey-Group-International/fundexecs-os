/**
 * Password reset — placeholder.
 *
 * The reset/set-password forms were part of the archived frontend
 * (`archive/frontend-2026-06-11`). The auth callback/confirm routes under
 * app/auth remain functional; rebuild this UI on the new design system.
 */
export default function ResetPasswordPage() {
  return (
    <main
      style={{
        minHeight: '100dvh',
        display: 'flex',
        alignItems: 'center',
        justifyContent: 'center',
        padding: '2rem',
        textAlign: 'center'
      }}
    >
      <p style={{ opacity: 0.7 }}>Password reset is being rebuilt.</p>
    </main>
  );
}

// app/(app)/design-system/page.tsx
// The design-system reference — renders the ComponentGallery (tokens,
// typography, and component styles). Auth is enforced by the (app) layout.
import { ComponentGallery } from "@/components/design-system/ComponentGallery";

export default function DesignSystemPage() {
  return <ComponentGallery />;
}
